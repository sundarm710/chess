# Python
engine/.venv/
__pycache__/
*.pyc
*.egg-info/
.mypy_cache/
.pytest_cache/

# Large public-data dumps (CLAUDE.md §4, §8) — never committed
data/dumps/

# Backend analysis artifacts / local store (FileFeatureStore default root)
engine/.data/

# Generated parity fixture (regenerated by run_tests.sh from golden_fens.py)
web/test/golden.json

# Source archive — extracted into data/candidates2026/, so the zip is redundant
Candidates2026.zip

# OS / editor
.DS_Store
