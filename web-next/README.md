# web-next — frontend spike

A **proof-of-concept** re-implementation of the Profiles view on a modern stack, to
compare against the vanilla `../web` app before committing to a migration. **`../web` is
untouched and remains the source of truth.**

## Stack

- **Vite 6** (rollup-based — works on Node 22.9; Vite 7/8 need Node 22.12+) + **React 19** + **TypeScript** (strict)
- **Tailwind v4** (`@tailwindcss/vite`) with the project design tokens (paper/oxblood/deep-blue, Fraunces/Hanken/IBM Plex Mono)
- **TanStack Table** — the player×feature matrix (sortable, colour-coded, sticky) for free
- **react-chartjs-2** — declarative Chart.js (no manual `destroy()`/recreate lifecycle)
- **Vitest** — unit tests for the pure view-logic

## What it demonstrates (vs the vanilla app)

| Concern | Vanilla `web/` | `web-next/` |
|---|---|---|
| Matrix (sort, colour, sticky) | hand-rolled in `matrix()` | `<Matrix>` via TanStack Table |
| Phase/colour slicing | `sliceValue` + manual re-render | typed `sliceValue` + React state |
| Chart lifecycle | manual `destroyCharts()` | `<Bar>` manages it |
| Data types | `(p.meta[id]||{})` guards | compile-checked `types.ts` |
| View-logic tests | — | `profile.test.ts` (Vitest) |

### Full parity with `../web`, two views

**Game / Profiles** tabs + deep links (`#profiles/<slug>`, `#<id>@<ply>`). The pure engine
(`src/engine/*.js` — feature engine, PGN parser, highlights, catalog, analysis) is **reused
verbatim** from `../web`; only chess.js is pinned to `0.10.3` (the parser depends on its API).
So the offline feature math can't drift from the canonical engine.

**Game view** — board (cburnett SVG, move + feature-evidence highlights), ←/→ stepper, the
category-grouped feature table (per-side values, coloured deltas, favour tally), the
explanation panel ("why it changed"), the both-sides trend chart, running aggregates
(exposures / avg space / worst-deficit + plain reading), the round/game picker, custom-PGN
paste, and the **Backend toggle** (analyze via `chesslab.api`, auto-fallback to offline).

**Profiles view** (redesigned, insight-first — not a 1:1 port):
- **Takeaway** sentence + **Matrix hero** (category group headers, sortable, sticky, focus).
- **Insight rail:** **Winning DNA** (top ↑/↓ result-correlations as diverging bars, full chart
  behind a toggle) + **Focus panel** (clicked feature's meaning + ranked players).
- **Player radar** (clustered), **Phase & colour** card (trajectory / fingerprint / White-vs-
  Black), and the **feature scatter**.

## Run

```bash
npm install
npm run dev        # predev copies ../web/data → public/data, then serves on :5174
npm test           # vitest
npm run build      # tsc -b && vite build
```

`public/data` is a git-ignored mirror of `../web/data` (regenerated by the Python
pipeline); `npm run sync-data` (run automatically by `predev`) refreshes it.

## Trade-off

This stack gives up "openable as a raw static file" (CLAUDE.md §11) in exchange for a
build step + typed, component-based, testable UI. Worth it if the app grows toward the
corpus explorer / style-space atlas; not worth it if it stays a single static instrument.
